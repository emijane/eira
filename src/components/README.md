# `src/components`

This folder contains shared components that are not specific to a single app route.

## Subfolders

- `ui/`
  Reusable UI primitives used across the app.

## How this folder connects

- Components here are imported by app-specific components in `src/app/components/`.
- Shared primitives help keep route-level components simpler and more consistent.
